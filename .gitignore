# Data (too large / not versioned)
data/raw/
data/processed/
data/*.csv
*.zip
*.parquet

# Vector store artifacts
vector_store/*
!vector_store/.gitkeep
# Task 2 deliverable: keep the persisted FAISS index + metadata sidecar (both <100MB)
!vector_store/index.faiss
!vector_store/chunks.parquet

# Local virtualenv
.venv/

# Reports figures are kept, but ignore heavy logs
reports/*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
.python-version

# OS / IDE
.DS_Store
Thumbs.db

# build artifact
reports/interim_report.html
